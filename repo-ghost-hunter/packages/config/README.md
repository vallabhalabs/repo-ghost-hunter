# @repo-ghost-hunter/config

Shared configuration for the Repo Ghost Hunter monorepo.

## Contents

- **tsconfig/** – Base TypeScript configs (e.g. `base.json`) for apps and packages
- **eslint/** – Shared ESLint config (e.g. `base.js`)

Apps and packages can extend these configs to keep tooling consistent across the repo.
